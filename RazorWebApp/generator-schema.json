{
  "source generator": "https://json-generator.com/#",
  [
    "{{repeat(12)}}",
    {
      "FirstName": "{{firstName()}}",
      "LastName": "{{surname()}}",
      "Age": "{{integer(16, 90)}}",
      "Emails": [
        "{{repeat(1, 3)}}",
        {
          "EmailAddress": "{{email()}}"
        }
      ],
      "Address": {
        "Street": "{{integer(100, 9999)}} {{street()}}",
        "City": "{{city()}}",
        "State": "{{state()}}",
        "ZipCode": "{{integer(10000,99999)}}-{{integer(1000,9999)}}"
      },
      "Tasks": [
        "{{repeat(0, 6)}}",
        {
          "Title": "{{lorem(1)}}",
          "Description": "{{lorem(1, \"paragraphs\")}}",
          "StartDate": "{{date()}}",
          "EndDate": "{{date()}}",
          "Priority": "{{integer(1, 4)}}"
        }
      ]
    }
  ]
}
